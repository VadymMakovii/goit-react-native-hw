import {
  Text,
  View,
} from "react-native";

import styles from "./ProfileScreen.styles";

const ProfileScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={{color: "tomato"}}>ProfileScreen</Text>
    </View>
  );
};

export default ProfileScreen;
